import { useEffect } from 'react';
import { get } from '../../utils/httpRequest';
import axios from 'axios';

const Home = () => {
    useEffect(() => {}, []);
    return (
        <div>
            <h1>home</h1>
        </div>
    );
};

export default Home;
